---
phase: 02-content-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/figma/Hero.tsx
  - src/components/figma/FigmaApp.tsx
  - src/locales/pl.json
  - src/locales/en.json
  - src/locales/de.json
  - src/locales/uk.json
  - src/locales/ru.json
  - src/locales/ar.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hero section shows two-column layout: text left, image right on large screens"
    - "A Badge component with 'Od 2008 roku' text appears above the main heading"
    - "Two buttons appear below the body text: one linking to #projekty, one linking to #kontakt"
    - "A floating accent card 'Aktywna pomoc / Ukraina & Bialorus' overlays the bottom-left of the hero image"
    - "A CTA section with dark background (bg-primary) appears after the main content sections"
    - "CTA section has Heart icon, heading, subheading, and two buttons"
    - "All new strings exist in all 6 locale files (pl, en, de, uk, ru, ar)"
  artifacts:
    - path: "src/components/figma/Hero.tsx"
      provides: "Redesigned hero with two-column layout, badge, buttons, floating card"
      min_lines: 60
    - path: "src/components/figma/FigmaApp.tsx"
      provides: "CTASection rendered after existing content sections"
    - path: "src/locales/pl.json"
      provides: "New hero.badge, cta.* translation keys"
      contains: "cta.heading"
  key_links:
    - from: "src/components/figma/Hero.tsx"
      to: "src/locales/pl.json"
      via: "useTranslation t() calls for badge and button text"
      pattern: "t\\('hero\\."
    - from: "src/components/figma/FigmaApp.tsx"
      to: "CTASection component"
      via: "rendered inside FigmaApp after sections"
      pattern: "CTASection"
---

<objective>
Redesign the Hero section to match the reference design (two-column, badge, floating card, action buttons) and add a CTA section below the main content. Wire all new strings into i18n translation files for all 6 languages.

Purpose: Improves the homepage first impression with the reference design layout. The CTA block provides a clear call to action for visitors.
Output: Updated Hero.tsx, new CTASection component inline in FigmaApp.tsx (or as a separate file), updated locale files.
</objective>

<execution_context>
@/home/lucas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lucas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/figma/Hero.tsx
@src/components/figma/FigmaApp.tsx
@src/locales/pl.json
@idea_for_new_design_main_page/components/hero-section.tsx
@idea_for_new_design_main_page/components/cta-footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new i18n keys for Hero badge and CTA section to all 6 locales</name>
  <files>src/locales/pl.json, src/locales/en.json, src/locales/de.json, src/locales/uk.json, src/locales/ru.json, src/locales/ar.json</files>
  <action>
    Add these keys to ALL 6 locale files. Polish is the canonical text; translate appropriately for each language.

    **New keys to add:**

    ```json
    "hero.badge": "Od 2008 roku",
    "hero.floatingLabel": "Aktywna pomoc",
    "hero.floatingValue": "Ukraina & Białoruś",
    "cta.heading": "Pomoc zaczyna się od jednego gestu",
    "cta.body": "Każde wsparcie ma znaczenie. Dołącz do nas i pomóż tym, którzy tego najbardziej potrzebują. Razem tworzymy Europę opartą na solidarności.",
    "cta.btnProjects": "Poznaj nasze projekty",
    "cta.btnContact": "Napisz do nas"
    ```

    **Translations by language:**

    English (en.json):
    - hero.badge: "Since 2008"
    - hero.floatingLabel: "Active aid"
    - hero.floatingValue: "Ukraine & Belarus"
    - cta.heading: "Help begins with a single gesture"
    - cta.body: "Every contribution matters. Join us and help those who need it most. Together we build a Europe founded on solidarity."
    - cta.btnProjects: "Explore our projects"
    - cta.btnContact: "Write to us"

    German (de.json):
    - hero.badge: "Seit 2008"
    - hero.floatingLabel: "Aktive Hilfe"
    - hero.floatingValue: "Ukraine & Belarus"
    - cta.heading: "Hilfe beginnt mit einer Geste"
    - cta.body: "Jede Unterstützung zählt. Schließen Sie sich uns an und helfen Sie denen, die es am meisten brauchen. Gemeinsam bauen wir ein Europa auf Solidarität."
    - cta.btnProjects: "Unsere Projekte entdecken"
    - cta.btnContact: "Schreiben Sie uns"

    Ukrainian (uk.json):
    - hero.badge: "З 2008 року"
    - hero.floatingLabel: "Активна допомога"
    - hero.floatingValue: "Україна та Білорусь"
    - cta.heading: "Допомога починається з одного жесту"
    - cta.body: "Кожна підтримка важлива. Приєднуйтесь до нас і допоможіть тим, хто цього найбільше потребує. Разом ми будуємо Європу солідарності."
    - cta.btnProjects: "Дізнатися про наші проєкти"
    - cta.btnContact: "Написати нам"

    Russian (ru.json):
    - hero.badge: "С 2008 года"
    - hero.floatingLabel: "Активная помощь"
    - hero.floatingValue: "Украина и Беларусь"
    - cta.heading: "Помощь начинается с одного жеста"
    - cta.body: "Каждая поддержка важна. Присоединяйтесь к нам и помогите тем, кто больше всего нуждается. Вместе мы строим Европу солидарности."
    - cta.btnProjects: "Узнать о наших проектах"
    - cta.btnContact: "Написать нам"

    Arabic (ar.json):
    - hero.badge: "منذ عام 2008"
    - hero.floatingLabel: "مساعدة نشطة"
    - hero.floatingValue: "أوكرانيا وبيلاروسيا"
    - cta.heading: "المساعدة تبدأ بإيماءة واحدة"
    - cta.body: "كل دعم مهم. انضم إلينا وساعد من يحتاجون إليه أكثر. معًا نبني أوروبا قائمة على التضامن."
    - cta.btnProjects: "اكتشف مشاريعنا"
    - cta.btnContact: "اكتب لنا"

    Keep all existing keys — only append the new ones at the end of each file's JSON object.
    IMPORTANT: Maintain valid JSON (no trailing commas on last key).
  </action>
  <verify>Run `node -e "JSON.parse(require('fs').readFileSync('src/locales/pl.json','utf8'))"` for each locale file to confirm valid JSON. All 6 files must parse without error.</verify>
  <done>All 6 locale files contain the 7 new keys (hero.badge, hero.floatingLabel, hero.floatingValue, cta.heading, cta.body, cta.btnProjects, cta.btnContact) and remain valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Hero component and add CTASection to FigmaApp</name>
  <files>src/components/figma/Hero.tsx, src/components/figma/FigmaApp.tsx</files>
  <action>
    **src/components/figma/Hero.tsx:**

    Rewrite the Hero component following the reference design (`idea_for_new_design_main_page/components/hero-section.tsx`) adapted for TanStack Start (no Next.js `Image`, no `Link` — use `<img>` and `onClick`/`<a>`).

    Layout structure:
    ```tsx
    <section className="relative overflow-hidden bg-gradient-to-br from-slate-50 via-blue-50 to-slate-100 py-20 pt-32">
      <div className="relative mx-auto max-w-6xl px-6 pb-16 pt-8">
        <div className="grid items-center gap-12 lg:grid-cols-2 lg:gap-16">
          {/* Text column */}
          <div className="flex flex-col gap-6">
            <Badge variant="secondary" className="w-fit border border-border text-xs font-medium tracking-wide text-muted-foreground">
              {t('hero.badge')}
            </Badge>
            <h1 className="text-4xl font-semibold leading-tight text-slate-900 sm:text-5xl lg:text-6xl">
              {t('hero.title')}
            </h1>
            <p className="text-lg font-medium tracking-wide text-primary/80 md:text-xl">
              {t('hero.tagline')}
            </p>
            {/* Body paragraphs — keep existing 6 body keys */}
            <div className="max-w-lg space-y-3 text-base leading-relaxed text-muted-foreground">
              <p>{t('hero.body1')}</p>
              <p>{t('hero.body2')}</p>
              <p className="font-medium text-slate-900">{t('hero.body3')}</p>
              <p className="font-medium text-slate-900">{t('hero.body4')}</p>
              <p>{t('hero.body5')}</p>
              <p className="font-medium text-slate-900">{t('hero.body6')}</p>
            </div>
            <div className="flex flex-wrap items-center gap-3 pt-2">
              <Button size="lg" onClick={() => onNavigate('projects')} className="gap-2">
                {t('hero.btnProjects')}
                <ArrowRight className="size-4" />
              </Button>
              <Button size="lg" variant="outline" onClick={() => onNavigate('contact')}>
                {t('hero.btnContact')}
              </Button>
            </div>
          </div>

          {/* Image column */}
          <div className="relative">
            <div className="relative aspect-[4/3] overflow-hidden rounded-2xl shadow-2xl shadow-foreground/5">
              <img
                src="/images/locations/data/zdjecie-o-fundacji.jpg"
                alt={t('hero.title')}
                className="h-full w-full object-cover"
              />
            </div>
            {/* Floating accent card */}
            <div className="absolute -bottom-6 -left-4 rounded-xl border border-border bg-card p-4 shadow-lg md:-left-8">
              <p className="text-xs font-medium uppercase tracking-widest text-muted-foreground">
                {t('hero.floatingLabel')}
              </p>
              <p className="mt-1 text-2xl font-semibold text-foreground">
                {t('hero.floatingValue')}
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
    ```

    Keep existing imports. Add `Badge` import from `@/components/ui/badge`. Remove `Card`, `CardContent` imports if no longer used. Keep `ArrowRight` from lucide-react. Keep `useTranslation`.

    **src/components/figma/FigmaApp.tsx:**

    Add a CTASection component (inline in FigmaApp.tsx or as a new file `src/components/figma/CTASection.tsx`) based on reference design `idea_for_new_design_main_page/components/cta-footer.tsx`. Adapt for TanStack Start (no `Link` — use `<a>` for mailto, `onClick={() => onNavigate('projects')}` for the projects button).

    CTASection structure:
    ```tsx
    function CTASection({ onNavigate }: { onNavigate: (section: string) => void }) {
      const { t } = useTranslation()
      return (
        <section className="relative overflow-hidden bg-primary py-20 md:py-28">
          <div className="relative mx-auto max-w-3xl px-6 text-center">
            <div className="mx-auto mb-6 flex h-14 w-14 items-center justify-center rounded-full bg-white/20">
              <Heart className="size-6 text-white" />
            </div>
            <h2 className="text-3xl font-semibold leading-tight text-primary-foreground md:text-5xl">
              {t('cta.heading')}
            </h2>
            <p className="mx-auto mt-5 max-w-xl text-base leading-relaxed text-primary-foreground/70">
              {t('cta.body')}
            </p>
            <div className="mt-8 flex flex-wrap items-center justify-center gap-3">
              <Button size="lg" variant="secondary" className="gap-2" onClick={() => onNavigate('projects')}>
                {t('cta.btnProjects')}
                <ArrowRight className="size-4" />
              </Button>
              <a href="mailto:kontakt@dwe.org.pl">
                <Button size="lg" variant="outline" className="border-primary-foreground/20 bg-transparent text-primary-foreground hover:bg-primary-foreground/10 hover:text-primary-foreground">
                  {t('cta.btnContact')}
                </Button>
              </a>
            </div>
          </div>
        </section>
      )
    }
    ```

    Import `Heart` from `lucide-react` in the file where CTASection is defined.

    In FigmaApp.tsx, render `<CTASection onNavigate={handleNavigate} />` after the existing sections (after contact/footer, but before the closing div). Read the current FigmaApp.tsx to find the exact insertion point — it should come after the main sections and before `</div>` of the main container, so it appears below the content but above the Footer.

    Pass `onNavigate` prop to CTASection — FigmaApp already has a `handleNavigate` function.
  </action>
  <verify>Run `pnpm tsc --noEmit` — no TypeScript errors. Start dev server (`pnpm dev`) and open http://localhost:3000 — hero section shows two-column layout with Badge and floating card visible, CTA section appears below the main sections with dark background.</verify>
  <done>Hero.tsx renders two-column layout with Badge "Od 2008 roku" above heading, floating accent card on image, two action buttons. CTASection renders with dark bg-primary background and Heart icon. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- All 6 locale files are valid JSON with new cta.* and hero.badge/floatingLabel/floatingValue keys
- Hero shows two-column layout at lg breakpoint, stacked on mobile
- Badge "Od 2008 roku" appears above heading
- Floating accent card overlays hero image bottom-left
- CTA section renders with bg-primary dark background and Heart icon below main sections
- Buttons in Hero and CTA navigate correctly (projects scroll, contact scroll/mailto)
</verification>

<success_criteria>
Homepage hero is redesigned to match the reference two-column layout with badge and floating accent card. CTA block appears after main content. All new UI strings are translated in all 6 languages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-polish/02-02-SUMMARY.md`
</output>
