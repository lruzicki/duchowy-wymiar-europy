---
phase: 02-content-polish
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [CMS-01, CMS-02, CMS-03, CMS-04, CMS-05, BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Browser tab shows 'Duchowy Wymiar Europy'"
    - "Hero has two-column layout with badge and floating accent card"
    - "CTA section visible with dark background below main content"
    - "Map pins show minimal popup with name and 'See more' link"
    - "Location page at /locations/dabrowa loads without errors"
    - "No DevTools panels in production build"
    - "No Unsplash URLs visible anywhere"
  artifacts:
    - path: "src/routes/__root.tsx"
      provides: "Correct title and meta tags"
    - path: "src/routes/locations.$slug.tsx"
      provides: "Working location detail page"
    - path: "src/components/figma/ProjectsMap.tsx"
      provides: "CMS-only map with no hardcoded data"
---

<objective>
Human verification of all Phase 2 Content & Polish work. Confirms that all visual, functional, and branding requirements are met before declaring Phase 2 complete.

Purpose: Catches visual or integration issues that automated checks miss. Single checkpoint after all 4 implementation plans complete.
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/home/lucas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lucas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Phase 2 visual and functional correctness</name>
  <what-built>
    All Phase 2 Content and Polish work:
    - Plan 01: Keystatic schema extended (images array, city, country, date, GitHub storage mode) + .env.example with GitHub OAuth credentials (CMS-05 Phase 2 portion)
    - Plan 02: Hero redesigned (two-column, badge, floating card) + CTA section added
    - Plan 03: Branding meta tags updated + DevTools gated to dev only
    - Plan 04: Hardcoded data removed, location pages created at /locations/[slug]
  </what-built>
  <action>Human verifies all Phase 2 work items according to the steps in how-to-verify.</action>
  <verify>All 8 verification points checked by human visual inspection — see how-to-verify for detailed steps.</verify>
  <how-to-verify>
    Start the dev server: `pnpm dev` (if not already running)

    1. **Browser tab title** — Open http://localhost:3000 — confirm tab shows "Duchowy Wymiar Europy" (not "TanStack Start Starter")

    2. **Hero redesign** — On the homepage:
       - Confirm two-column layout visible on desktop (text left, image right)
       - Confirm Badge "Od 2008 roku" appears above the main heading
       - Confirm floating card "Aktywna pomoc / Ukraina & Białoruś" overlays the hero image at bottom-left
       - Confirm two buttons below body text: "Poznaj nasze projekty" and "Kontakt"

    3. **CTA section** — Scroll down past the main sections:
       - Confirm a dark-background section appears with Heart icon
       - Confirm heading "Pomoc zaczyna się od jednego gestu" is visible
       - Confirm two buttons: "Poznaj nasze projekty" and "Napisz do nas"

    4. **Map pins** — Scroll to the map section:
       - Confirm pins appear on the map (at least the Dabrowa test pin)
       - Click a pin — confirm a small popup opens with location name and "Zobacz więcej" link
       - Confirm no full-screen dialog/modal opens on pin click (old behavior removed)

    5. **Location page** — Click "Zobacz więcej" in the pin popup (or navigate directly to http://localhost:3000/locations/dabrowa):
       - Confirm the page loads without errors
       - Confirm "Powrót do mapy" button is visible
       - Confirm location name is displayed
       - Confirm image area shows (even if empty/placeholder, it should not crash)
       - Click "Powrót do mapy" — confirm it returns to the homepage

    6. **DevTools** — Confirm TanStack DevTools panel IS visible in the bottom-right corner (we're in dev mode — this is correct)

    7. **No Unsplash images** — Inspect images on the page; none should be from images.unsplash.com

    8. **Language check** — Use the language switcher and confirm hero badge and CTA section text change language (or if auto-detect, confirm Polish is default)

    Report any issues with the specific plan number that should be revisited (01-04).
  </how-to-verify>
  <done>Human types "approved" confirming all 8 verification points pass.</done>
  <resume-signal>Type "approved" to declare Phase 2 complete, or describe any issues found (e.g. "Plan 02: floating card not visible on mobile")</resume-signal>
</task>

</tasks>

<verification>
All 8 verification points above confirmed by human visual inspection.
</verification>

<success_criteria>
Phase 2 declared complete when:
- Browser tab shows correct organization name
- Hero shows two-column layout with badge and floating card
- CTA section renders with dark background
- Map pins show minimal popup linking to location pages
- Location pages load without errors and show back button
- All new strings are translated and switch with language selector
- .env.example exists with all GitHub OAuth credential names (CMS-05 Phase 2 portion confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-polish/02-05-SUMMARY.md`
</output>
